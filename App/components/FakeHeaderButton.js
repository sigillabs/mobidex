import React, { Component } from 'react';
import { View } from 'react-native';

export default class FakeHeaderButton extends Component {
  render() {
    return <View style={{ width: 50, height: 50 }} />;
  }
}
